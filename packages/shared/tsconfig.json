{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "rootDir": "./src",
    "outDir": "./dist"
  },
  "include": ["src/**/*"],
  // Test không đi vào bản build. Việc kiểm kiểu cho test do tsconfig.test.json lo.
  "exclude": ["dist", "node_modules", "src/**/*.test.ts"]
}
